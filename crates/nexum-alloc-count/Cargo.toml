[package]
name = "nexum-alloc-count"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Nexum: a counting global allocator for allocation profiling (Phase 21.5 instrumentation only)."

# Deliberately does NOT inherit [workspace.lints]: this crate's sole purpose
# is a `#[global_allocator]` (which requires `unsafe impl GlobalAlloc`). The
# workspace default is `unsafe_code = "allow"`, but unsafe stays confined to
# this crate plus nexum-network/spsc.rs and nexum-wasm/linker_cache.rs; keep
# it that way.
[dependencies]
